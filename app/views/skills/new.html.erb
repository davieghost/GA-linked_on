<%= form_for @skill do |s| %>
	<%= s.label "Skill Name" %>
	<%= s.text_field :name %>
	<%= s.select:user_id, options_from_collection_for_select(User.all, :id, :name) %>
<br><br>
	<%= s.submit "MakeIT!" %>

<% end %>