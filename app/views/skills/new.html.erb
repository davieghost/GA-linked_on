<%= form_for @skill do |f| %>
	<%= f.label "Skill Name" %>
	<%= f.text_field :name %>
	<%= f.select:user_id, options_from_collection_for_select(User.all, :id, :name) %>
<br><br>
	<%= f.submit "MakeIT!" %>

<% end %>